import { StyleSheet } from "react-native";

export const weightPlateStyles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
  },
  plateWrapper: {
    width: 80,
    height: 80,
    marginHorizontal: 12,
    alignItems: "center",
    justifyContent: "center",
  },
  weightText: {
    fontFamily: "BebasNeue",
    fontSize: 44,
    color: "#0a0a0a",
    letterSpacing: 1.32,
    textAlign: "center",
  },
});
